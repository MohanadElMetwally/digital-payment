services:
  backend:
    image: 921040037119.dkr.ecr.eu-central-1.amazonaws.com/digital_payment:${IMAGE_TAG:-latest}
    restart: always
    env_file:
      - .env.stg
    build:
      context: .
    ports:
      - "8080:8080"
